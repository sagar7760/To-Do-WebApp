# Production environment variables for frontend
# Set these in Vercel Dashboard -> Settings -> Environment Variables
# For Vercel, set VITE_BACKEND_URL in the project's Environment Variables settings.
# This file is used for local production builds, not by Vercel directly.